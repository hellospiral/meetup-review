<h2>{{model.meetup.name}}</h2>
<img src="{{model.meetup.image}}">
<h4>{{model.meetup.description}}</h4>
<a href="{{model.url}}"></a>
<hr>
<button class="btn btn-primary btn-lg" {{action "addToMyMeetups" model.meetup}}>Add to myMeetups</button>
{{review-list meetup=model.meetup}}
{{new-review meetup=model.meetup save2="save3"}}
